const PunctuationQuestion = (name) => {
  return `${name} wants to go to the beach, but it is raining a lot.`;
};

const FindAdjectivesQuestion = (name) => {
  const adjectives = ["beautiful", "big", "delicious"];
  // Add buttons here
  return `${name} can make beautiful big cakes. Everyone says they are delicious too.`;
};

const RightChoiceQuestion = (name) => {
  const adjectives = ["smarter", "smartest"];
  // Add buttons here
  return `${name} was the smart / smartest kid in school.`;
};

// https://www.noredink.com/curriculum/practice/all
